//TODO: This component will display the list of journal entries. It will use Redux actions and reducers to fetch the journal entries from the server. Dispatch an action to trigger the API call and update the Redux store with the retrieved entries. Then, use the Redux store to display the entries. It will Iterate over the entries in the EntryList component and map each entry to a suitable UI component for display. A separate component called EntryItem will represent an individual entry. Pass the necessary data to the EntryItem component as props. Additionally, this will have an infinite scroll feature that will load more entries as the user scrolls down the page.