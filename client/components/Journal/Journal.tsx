//TODO: This component will display a search bar that allows the user to search for a journal entry by title, moon phase, date, description or keyword. Once the query has been submitted it will display the relevant results by rendering the EntryList component. It will use Redux actions and reducers to fetch the journal entries from the server. Dispatch an action to trigger the API call and update the Redux store with the retrieved entries. Then, use the Redux store to display the entries. It will Iterate over the entries in the EntryList component and map each entry to a suitable UI component for display. A separate component called EntryItem will represent an individual entry. Pass the necessary data to the EntryItem component as props.